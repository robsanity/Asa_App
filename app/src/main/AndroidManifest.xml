<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Offerta"></activity>
        <activity android:name=".UpFile" />
        <activity android:name=".UpAnnuncio" />
        <activity android:name=".FileCaricati" />
        <activity android:name=".Chat" />
        <activity android:name=".Profilo" />
        <activity android:name=".Richiesta" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".WelcomeActivity" />
    </application>

</manifest>